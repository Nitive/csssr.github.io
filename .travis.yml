language: ruby

install:
  - bundle install

script:
  - bundle exec jekyll build

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  target-branch: master
  keep-history: true
  local-dir: _site
  on:
    branch: stable
